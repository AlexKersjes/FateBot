module.exports = {
	name: 'look',
	description: 'Look around.',
	aliases: ['investigate', 'explore', 'inspect'],
	visibleReject: true,
	disabled: true,
	execute(message, args, client)
	{
		// TODO: make a more generic version of look which loads data from a file, rather than hardcoding.
	},
};